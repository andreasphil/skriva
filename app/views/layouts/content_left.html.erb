<%= render layout: 'layouts/master' do |f| %>
  <!-- Left column -->
  <div class="content-wrapper">
    <%= yield %>
  </div>

  <!-- Greyed-out right column -->
  <div class="overlay-wrapper"></div>
<% end %>
