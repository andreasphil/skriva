<!DOCTYPE html>
<html>
  <head>
    <title>Skriva</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>
  <body>
    <header id="header" class="navbar">
      <section class="navbar-section">
        <%= link_to 'Skriva', home_index_path, class: ['navbar-brand', 'mr-2'] %>
        <% if signed_in? %>
          <%= link_to 'Edit', notes_index_path, class: ['btn', 'btn-link'] %>
          <%= button_to t('.sign_out'),
            sign_out_path,
            method: :delete, class: ['btn', 'btn-link'] %>
        <% else %>
          <%= link_to t('.sign_in'), sign_in_path, class: ['btn', 'btn-link'] %>
        <% end %>
      </section>
    </header>

    <!-- TODO: Style flashes -->
    <div id="flash">
      <% flash.each do |key, value| %>
        <div id="flash_<%= key %>"><%=h value %></div>
      <% end %>
    </div>

    <%= yield %>
  </body>
</html>
