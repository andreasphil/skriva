<% unless flash.empty? %>
  <div class="notification__wrapper"
    data-stimulus-controller="notifications">
    <% flash.each do |key, value| %>
      <div class="notification notification--<%= key %> notification--interactive"
        data-stimulus-action="click->notifications#close">
        <%= value %>
      </div>
    <% end %>
  </div>
<% end %>
